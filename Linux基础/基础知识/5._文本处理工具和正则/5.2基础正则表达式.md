# 元字符：

-   `.`：**匹配任意单个**字符，但不能匹配换行符`\n`
-   `*`：它前面的正则表达式需执行**0或多次匹配操作**
-   `?`：它前面的正则表达式需执行**0或1次匹配操作**
-   `+`：它前面的正则表达式需执行**1次以上的匹配操作**
-   `{M,N}`：它前面的正则表达式需执行**至少M次至多N次匹配操作**
-   `{M,}`：它前面的正则表达式需执行**至少M次匹配操作**
-   `{,N}`：它前面的正则表达式需执行**至多N次匹配操作(最少当然是0次)。**注意，perl正则不支持这种方式
-   `{M}`：它前面的正则表达式需执行**M次匹配操作**
-   锚定：锚定的意思是匹配位置，而非匹配字符实体

-   `^`：匹配行首位置，注意匹配的是位置，不是字符
-   `$`：匹配行尾位置，注意匹配的是位置，不是字符

-   特殊且常用的的组合正则表达式：
-   `^$`：它表示匹配空行
-   `.*`：匹配任意长度的任意字符，但不能匹配换行符。真正的匹配任意长度的任意字符，见下面

```markdown
[abc]{3,5}  # 表示abc任意字符都可以出现，比如全是a，或者ab同时出现，但总的出现次数为3-5次
.*          # 表示任一字符(除换行符)，可以任意出现任意次数，它不表示a之后就必须全是a
.+          # 表示任一字符(除换行符)，可以任意出现至少一次，它不表示a之后就必须全是a
```

| **元字符** | **作用** | **示例** | **匹配示例** |
| --- | --- | --- | --- |
| `.` | 任意字符（换行符除外） | `a.b` | "acb", "a7b" |
| `^` | 行首匹配 | `^Hello` | "Hello world"（匹配） |
| `$` | 行尾匹配 | `end$` | "this is the end"（匹配） |
| `*` | 0 次或多次 | `ab*c` | "ac", "abc", "abbc" |
| `+` | 1 次或多次 | `ab+c` | "abc", "abbc" |
| `?` | 0 次或 1 次 | `ab?c` | "ac", "abc" |
| `{n}` | 恰好 n 次 | `a{3}` | "aaa" |
| `{n,}` | 至少 n 次 | `a{2,}` | "aa", "aaa" |
| `{n,m}` | n 到 m 次 | `a{2,4}` | "aa", "aaa", "aaaa" |
| `[]` | 字符集合，匹配其中任意一个字符 | `[aeiou]` | "a", "e", "i", "o", "u" |
| `[^]` | 取反，不匹配集合中的字符 | `[^0-9]` | 任何非数字字符 |
| `()` | 分组 | `(ab)+` | "ab", "abab" |
| `\` | 转义字符 | `\.` | 仅匹配 `.` 本身 |

# 中括号

中括号表示的是匹配任意一个，一般它和字符集的排序规则有关，不同工具采取的排序规则可能也不一样。

-   `[abcd...]`：匹配中括号内的任意一个字符
-   `[^abcd...]`：拒绝匹配中括号内的任意字符
-   `[a-z]`：匹配字母a到z
-   `[A-Z]`：匹配字母A到Z
-   `[0-9]`：匹配0-9，也就是匹配数字

关于字母的排序：

-   perl中，A-Z排在a的前面，所以`[A-z]`表示所有大小写字母
-   grep中，A-Z排在z的后面，所以`[a-Z]`表示所有大小写字母
-   还有些工具中，大小写的排序规则是aAbBcC…zZ，所以`[a-C]`表示aAbBcC共6个字母

| **正则** | **匹配内容** |
| --- | --- |
| `[abc]` | **匹配 **`a`**、**`b`** 或 **`c` 中的一个字符 |
| `[0-9]` | **匹配 0-9 之间的任意一个数字** |
| `[A-Z]` | **匹配 A-Z 之间的任意一个大写字母** |
| `[a-z]` | **匹配 a-z 之间的任意一个小写字母** |
| `[A-Za-z]` | **匹配任意大小写字母** |
| `[13579]` | **匹配奇数 1、3、5、7、9** |
| `[02468]` | **匹配偶数 0、2、4、6、8** |

## **（2）取反** `[^...]`

`[^...]` 表示 **匹配不在中括号内的字符**：

| **正则** | **匹配内容** |
| --- | --- |
| `[^aeiou]` | **匹配除 aeiou 之外的所有字符** |
| `[^0-9]` | **匹配非数字的字符** |
| `[^A-Z]` | **匹配非大写字母的字符** |

# 字符类

这是你请求的表格，包含正则表达式字符类及其示例：

| 字符类 | 描述 | 示例 |
| --- | --- | --- |
| `[:alpha:]` | 匹配字母（等价于 `[a-zA-Z]`） | `a`, `Z`, `k` |
| `[:digit:]` | 匹配数字（等价于 `[0-9]`） | `1`, `9`, `3` |
| `[:xdigit:]` | 匹配十六进制数（等价于 `[0-9a-fA-F]`） | `a`, `F`, `1`, `9` |
| `[:upper:]` | 匹配大写字母（等价于 `[A-Z]`） | `A`, `B`, `Z` |
| `[:lower:]` | 匹配小写字母（等价于 `[a-z]`） | `a`, `b`, `z` |
| `[:alnum:]` | 匹配数字或字母（等价于 `[0-9a-zA-Z]`） | `a`, `9`, `X`, `z` |
| `[:blank:]` | 匹配空白字符（空格或制表符） | ``（空格），`\t`（制表符） |
| `[:space:]` | 匹配所有空白字符（空格、制表符、换行符、回车符等） | ``（空格），`\t`（制表符），`\n`（换行符），`\r`（回车符） |
| `[:punct:]` | 匹配标点符号 | `!`, `"`, `@`, `,`, `.` |

## 预定义字符类

| 预定义字符类 | 描述 | 示例 |
| --- | --- | --- |
| `\d` | 匹配一个数字字符，等价于 `[0-9]` | `0`, `5`, `9` |
| `\D` | 匹配一个非数字字符，等价于 `[^0-9]` | `a`, `Z`, `!` |
| `\w` | 匹配一个字母数字字符或下划线，等价于 `[a-zA-Z0-9_]` | `a`, `1`, `_` |
| `\W` | 匹配一个非字母数字字符或下划线，等价于 `[^a-zA-Z0-9_]` | `@`, `!`, `#` |
| `\s` | 匹配一个空白字符，包括空格、制表符、换行符等 | ``（空格），`\t`（制表符），`\n`（换行符） |
| `\S` | 匹配一个非空白字符 | `a`, `9`, `!` |
| `\b` | 匹配一个单词边界 | `'word'`（在单词与非单词之间） |
| `\B` | 匹配一个非单词边界 | `a`（在单词内部） |
| `\0` | 匹配字符 `NULL`（ASCII 0） | `\0` |

# 反斜线序列

以下是包含常见反斜线序列的表格，包含它们的描述和示例：

| 反斜线序列 | 描述 | 示例 |
| --- | --- | --- |
| `\\` | 匹配反斜线字符 `\` | `\\` |
| `\d` | 匹配一个数字字符，等价于 `[0-9]` | `5`, `9`, `0` |
| `\D` | 匹配一个非数字字符，等价于 `[^0-9]` | `a`, `Z`, `!` |
| `\w` | 匹配一个字母数字字符或下划线，等价于 `[a-zA-Z0-9_]` | `a`, `1`, `_` |
| `\W` | 匹配一个非字母数字字符或下划线，等价于 `[^a-zA-Z0-9_]` | `@`, `#`, `!` |
| `\s` | 匹配一个空白字符，包括空格、制表符、换行符等 | ``（空格），`\t`（制表符），`\n`（换行符） |
| `\S` | 匹配一个非空白字符 | `a`, `9`, `!` |
| `\b` | 匹配一个单词边界 | `"hello"`（在单词和非单词边界之间） |
| `\B` | 匹配一个非单词边界 | `a`（在单词内部） |
| `\0` | 匹配字符 `NULL`（ASCII 0） | `\0` |
| `\t` | 匹配制表符（Tab） | `\t` |
| `\n` | 匹配换行符（Newline） | `\n` |
| `\r` | 匹配回车符（Carriage Return） | `\r` |
| `\f` | 匹配换页符（Form Feed） | `\f` |
| `\v` | 匹配垂直制表符（Vertical Tab） | `\v` |
| `\xhh` | 匹配指定的十六进制字符（`hh` 为两位十六进制数） | `\x41`（匹配字符 `A`） |
| `\u{hhhh}` | 匹配指定的 Unicode 字符（`hhhh` 为四位十六进制数） | `\u{03A9}`（匹配希腊字母 `Ω`） |

# 分组捕获和反向引用

基础正则中，使用括号可以对匹配内容进行分组并暂时保存，分组后会有分组编号，可以使用反斜线加编号`\N`的方式反向引用这些分组。

分组编号的方式是从左向右计算括号数，无论如何嵌套，第一个左括号对应的分组一定是编号1，用`\1`来引用，第二个左括号对应的分组一定是编号2，用`\2`来引用，依此类推。

例如grep的分组捕获：匹配两个连续相同的字母。

```markdown
echo "abcddefg" | grep -E "(.)\1"

● grep -E：使用扩展正则表达式（ERE）。
● (.)：匹配任意一个字符，并捕获它。
● \1：表示引用前面捕获的字符，即匹配 两个相同的连续字符。
```

可以认为分组就是变量赋值的过程。例如，上面示例的匹配过程如下：  
1.匹配第一个字母a，放进分组，即赋值给变量(假设变量名为`$1`)，即`$1="a"`，再继续执行正则表达式匹配过程的反向引用，它引用的是`$1`，于是表示第一个字母a后面还要是字母a，于是匹配失败。  
2.匹配第二个字母b，放进分组，即`$1="b"`，再匹配后一个字母，于是匹配失败。  
3.字母c同样如此。  
4.匹配字母d，放进分组，即`$1="d"`，再匹配后一个字母，发现匹配成功，于是`$1`被保存下来。  
5.已经匹配成功，于是结束。

# 二选一

-   `pattern1 | pattern2`：匹配竖线左边，或者匹配竖线右边都算匹配成功

关于二选一的结构，几点需要说明：

1.  因为竖线元字符的优先级很低，所以`ab|cd`匹配的是`ab`或`cd`，而不是abd或acd。
2.  成功匹配了左边，就不会再去对右边进行匹配。
3.  反向引用失败问题：竖线将两边的分组隔开，右边的永远无法反向引用左边的分组

在二选一结构种，两个反向引用问题的典型例子：

例如`a(.)|b\1`将无法匹配`ba`，因为评估了左边就不会评估右边。

例如`([ac])e\1|b([xyz])\2t`的左边能匹配aea或cec，但不能匹配cea或aec，右边能匹配bxxt或byyt或bzzt。但如果将`\2`换成`\1`，即`([ac])e\1|b([xyz])\1t`，将无法匹配`b[xyz]at`或`b[xyz]ct`，因为第一个分组括号在左边，无法参与右边的正则评估。