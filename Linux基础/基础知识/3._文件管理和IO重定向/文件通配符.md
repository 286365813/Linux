# 文件通配符

```bash
*		： 匹配任意长度的任意字符
？	： 匹配单个字符
[ ]	： 匹配中括号里面的任意字符
[^xxx]	:匹配除了xxx以外的任意字符  
```
```bash
[:digit:]：任意数字，相当于0-9 
[:lower:]：任意小写字母,表示 a-z
[:upper:]: 任意大写字母,表示 A-Z 
[:alpha:]: 任意大小写字母
[:alnum:]：任意数字或字母
[:blank:]：水平空白字符
[:space:]：水平或垂直空白字符
[:punct:]：标点符号
[:graph:]：图形字符
[:xdigit:]：十六进制字符
```